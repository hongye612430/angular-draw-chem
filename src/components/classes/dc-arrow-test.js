describe("DCArrow service tests", function () {
	beforeEach(module("mmAngularDrawChem"));

	var Arrow;

	beforeEach(inject(function (_DCArrow_) {
		Arrow = _DCArrow_.Arrow;
	}));

	it("should create a new Arrow object", function () {

	});
});
